# -----------------------------
# Global Git ignore for GES project
# Covers: Spring Boot (Maven) backend + Vite/React (pnpm) frontend
# -----------------------------

# --- Java / Maven / Spring ---
/target/
/out/
*.class
*.jar
*.war
*.ear
hs_err_pid*
*.log

# Maven wrapper (keep the wrapper jar if you commit it) - don't ignore the wrapper directory entirely unless intentional
# /.mvn/wrapper/maven-wrapper.jar

# IDE & build metadata
*.iml
/.idea/
.classpath
.project
.settings/

# Spring Boot specific logs
spring.log

# --- Node / Frontend (Vite + pnpm) ---
/node_modules/
/dist/
/build/

# Vite cache
/.vite/

# Environment variables (local secrets)
.env
.env.local
.env.*.local

# pnpm
.pnpm-store/
.pnpm/

# Lockfiles: commit pnpm-lock.yaml for deterministic installs. Uncomment to ignore if you intentionally don't want lockfiles.
# pnpm-lock.yaml

# --- Editor / OS ---
.DS_Store
Thumbs.db
*.swp

# VS Code
/.vscode/

# npm/yarn/pnpm debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
